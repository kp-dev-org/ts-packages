{
  "name": "kp-ts-utilities",
  "version": "1.0.5",
  "description": "TypeScript package for interfaces, abstract classes, and validation schemes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^24.3.3"
  }
}
